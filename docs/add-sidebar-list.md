# サイドバーの会話一覧を追加

## 概要

全てのログを一度に表示すると非常に画面が重くなる可能性がある。この問題を解消する。

## 要件

- 会話一覧表示と会話詳細表示を分ける
- 左サイドバーに会話一覧を標示させる
  - スマートフォンの場合は左側の Drawer で一覧を標示させる
- 会話詳細表示では一件の会話のみを
- 一覧の項目の表示名は、ユーザーの最初のメッセージを使用する
